const defaultUrl = 'ws://localhost:8008'
const defaultBroker = 'default'
const defaultUserName = 'default'
const defaultPassword = 'default'
const defaultPublishTopic = 'stm/cli/topic'
const defaultSubscribeTopic = 'stm/cli/topic'
const defaultRequestTopic = 'stm/cli/request'
const defaultMessage = 'Hello from Solace Try-Me CLI Publisher'
const defaultRequestMessage = 'Hello request from Solace Try-Me CLI Requestor'
const defaultReplyMessage = 'Hello reply from Solace Try-Me CLI Replier'
const defaultPublisherDescription = 'Publisher created via Solace Try-Me CLI'
const defaultReceiverDescription = 'Receiver created via Solace Try-Me CLI'
const defaultReplierDescription = 'Replier created via Solace Try-Me CLI'
const defaultCount = 1
const defaultInterval = 1000
const defaultLogLevel = 'ERROR'
export {
  defaultUrl,
  defaultBroker,
  defaultUserName,
  defaultPassword,
  defaultPublishTopic,
  defaultSubscribeTopic,
  defaultRequestTopic,
  defaultMessage,
  defaultRequestMessage,
  defaultReplyMessage,
  defaultPublisherDescription,
  defaultReceiverDescription,
  defaultReplierDescription,
  defaultCount,
  defaultInterval,
  defaultLogLevel
}
