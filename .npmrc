# Prevent lifecycle script execution by default
ignore-scripts=true

# Use lockfile strictly
package-lock=false

# Audit level for vulnerabilities
audit-level=moderate

# Note: This project uses Yarn. Please use 'yarn install --frozen-lockfile'
